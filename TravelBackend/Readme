all apis
url=http://localhost:3000
1- url/api/user/getusers
2-url/auth/signup
3--url/auth/signout
4--url/api/travel-story/getall

// npm i dotenv mongooes 



Q1-when 400 and when 404 
Ans-  400- when wrong data or missing data 
404- when route wrong 

Q2- why we use jwt.sign()
ans->=  jwt.sign() creates a secure authentication token by embedding the user’s ID inside a signed payload using a secret key, allowing the server to verify the user on future requests without re-authentication.

Q3-What is the difference?

find() → returns everything (unsafe for private data)

find({ userId }) → returns only logged-in user’s data (secure)


Q4- what is ESM model 

Interview-Ready Explanation

ESM (ECMAScript Modules) is JavaScript’s official module system where imports are statically analyzed and executed before runtime code. This improves performance and predictability but requires environment variables to be loaded before module imports.


simple-- it run all the important file before dotenv




Q5-🔹 How to Know You Are Using ESM
A- we are using in pakage.json {
  "type": "module"
}




































